{
  "name": "site-dns-tests",
  "version": "1.0.0",
  "description": "DNS verification tests for registrar settings",
  "type": "module",
  "scripts": {
    "verify": "node scripts/verify-dns.js",
    "propagation": "node scripts/check-propagation.js",
    "test": "npx playwright test",
    "deploy:vercel": "cd site && npx vercel --prod",
    "deploy:netlify": "cd site && npx netlify deploy --prod --dir=."
  },
  "dependencies": {
    "dns2": "^2.1.0",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0"
  }
}
